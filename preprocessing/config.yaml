# Raw packaging defaults used by run_all.py.
# Train/cache paths, split seed and validation fraction live in training/config/base.yaml.
# Fixed modality order and canonical labels are enforced by the data contract.
data:
  raw_root: E:/STUDY/DATASET/MyoPS380/Raw_data
normalization: unit255
label_order: legacy  # Input only; newly packaged cache is canonical.
